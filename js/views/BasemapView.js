var BasemapView = Backbone.View.extend({
    el: null,
    // template: Handlebars.templates['activityViewTpl'],
    events: {
    },
    initialize: function() {
    },
    render: function() {
        if (verbose == true) {
            console.log("rendering BasemapView")
        }
        
        return this
    }
});